"use client";

// This component is no longer needed as we've removed internationalization
// Keeping a minimal version that does nothing for backward compatibility

export function LanguageSwitcher() {
  // No-op language switcher for English-only app
  return null;
}
